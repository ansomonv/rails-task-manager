<br>
  <div>
    <h2><%= @task.title %></h2>
    <p><%= @task.details %></p>
    <p><%= @task.completed %></p>
  </div>

<%= link_to "éditer la tache", edit_task_path(@task) %>

<%= link_to "destroy", task_path(@task), method: :delete, data: {confirm: "are you sure?"} %>
